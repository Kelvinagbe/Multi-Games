{
  "name": "my-html-project",
  "version": "1.0.0",
  "scripts": {
    "build": "posthtml *.html */*.html -o dist/"
  },
  "devDependencies": {
    "posthtml": "^0.16.6",
    "posthtml-modules": "^0.9.0"
  }
}